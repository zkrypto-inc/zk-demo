# zkWallet Custody Financial Spec v1

## 목적
- `zkWallet Custody` 데모 페이지를 금융기관용 기준으로 설계하되, 내부적으로 `수탁 운영용`과 `스테이블코인 운영용`을 함께 다룬다.
- 이 문서는 `design` 폴더 아래의 공식 설계 초안으로 사용한다.
- 금융기관용 안에서도 다시 `플랫폼 운영자용`, `수탁 운영용`, `발행사용`, `개인 사용자용`으로 분리해 Use Case를 정의한다.
- 각 Use Case는 `사용자 화면`, `화면 설명 / 처리 개요`, `진행 단계`, `설명` 4개 영역으로 보여주는 것을 기본 원칙으로 한다.

## 문서 사용 원칙
- 이 문서는 `requirements`를 그대로 옮기는 문서가 아니라, 실제 데모 페이지에서 `무엇을 보여줄지`를 정하는 설계 문서다.
- 제품 소개는 `기능`만 나열하지 않고, `actor별 사용 흐름` 안에서 기능이 어떻게 드러나는지를 기준으로 정리한다.
- 금융기관용 안에서도 `플랫폼 운영자용`, `수탁 운영용`, `발행사용`, `개인 사용자용`을 분리해 봐야 하며, 같은 기능이라도 actor가 다르면 화면과 설명도 달라져야 한다.
- 기술 내부 구조보다 `이 화면에서 사용자가 무엇을 이해해야 하는가`가 우선이다.
- 시스템 처리 흐름은 꼭 다이어그램일 필요는 없으며, 기획자와 사용자 관점에서 `무슨 요청이 진행 중이고 어떤 상태가 보이는지`가 먼저 이해되면 된다.

## 1. 이번 문서의 범위

### 상위 제품 구조
- 거래소용
- 금융기관용

### 이번 문서에서 다루는 범위
- 금융기관용
  - 공통 온보딩 / 관리자 지정
  - 플랫폼 운영자용
  - 수탁 운영용
  - 발행사용
  - 개인 사용자용

### 이번 문서에서 보류하는 것
- 거래소용 상세 설계
- 지역화폐/바우처 시나리오
- STO 전체 독립 스토리
- VASP 제휴 리스크 관리 독립 스토리

### 이번 문서의 산출물
- 금융기관용 데모의 상위 구조
- actor별 Use Case 구분
- 각 Use Case의 데모 화면 spec
- 이후 페이지 설계로 내려갈 수 있는 기준선

## 2. 데모 페이지 공통 프레임

### 공통 레이아웃
- 좌측: `사용자 화면`
- 중앙: `화면 설명 / 처리 개요`
- 우측 상단: `진행 단계`
- 우측 하단: `설명`
- 선택: `기술 근거 패널`

### 화면 타입
- 운영자용:
  - 웹 대시보드 / 관리자 콘솔
- 개인 사용자용:
  - 슈퍼앱 내 앱형 화면

### 화면 설명 / 처리 개요 원칙
- 꼭 sequence diagram일 필요는 없다.
- 먼저 `이 화면이 무엇을 위한 화면인지`, `지금 어떤 상태인지`, `다음에 무엇이 일어나는지`를 설명한다.
- 내부 시스템명은 필요할 때만 보조적으로 붙인다.
- `Vault`, `party node`, `auxinfo` 같은 내부 용어는 기본 화면 설명에 노출하지 않고, 기술 근거 패널이나 내부 참고로만 남긴다.

### 우측 패널 원칙
- `진행 단계`
  - 현재 step
  - 전체 step 수
  - 이전/다음 단계 상태
- `설명`
  - 현재 step에서 사용자가 무엇을 했는지
  - 지금 화면에서 무엇을 확인해야 하는지
  - 다음 단계에서 무엇이 이어지는지
  - 데모에서 과장하면 안 되는 경계가 무엇인지

## 3. 금융기관용 구조

### 내부 분해
- `공통 기반`
  - tenant 생성
  - 관리자 지정
  - 권한 / audit context 설정
- `플랫폼 운영자용`
  - 테넌트 생성
  - 역할 관리
  - 발행사 / 수탁사 연결
- `수탁 운영용`
  - 법인 사용자 등록
  - 수탁 지갑 개설
  - 수탁 입금
  - 수탁 출금
- `발행사용`
  - 발행사 관리자
  - 승인자 / 준법감시인
  - 발행 / 소각 / 감사 대응
- `개인 사용자용`
  - 실제 지갑을 사용하는 개인 사용자
  - TX 요청과 서명 결과를 받는 흐름

### 내부 옵션
- 발행사 관리자 지갑 / 관리자 액션
- cold / hot wallet 구분
- key refresh / reshare
- HSM / Private Cloud / Signer 옵션

## 4. actor 정의

| actor | 설명 |
|---|---|
| 개인 사용자 | 지갑 생성, 키 생성, 거래 서명 요청의 주체 |
| 법인 사용자 | 수탁 서비스 가입, 수탁 입출금 요청의 요청 주체 |
| 수탁 운영자 | 수탁 지갑 개설, 입금 반영, 출금 요청 처리의 운영 주체 |
| 플랫폼 운영자 | tenant 할당, 발행사/수탁사 연결, 운영 권한 설정 주체 |
| 발행사 관리자 | 발행, 소각, 준비금 관련 요청 생성 주체 |
| 승인자 / 준법감시인 | 발행·소각·관리자 액션 승인 주체 |
| 감사자 | audit log, 요청 이력, 결과 이력 확인 주체 |
| IT / 보안 아키텍트 | Private Cloud, HSM, signer 옵션 검토 주체 |

## 5. 시스템 경계 메모

- 수탁 운영용과 스테이블코인 운영용 시나리오는 기본적으로 `고객 플랫폼 + Wallet Service(zkMPC)` 또는 `SC Lifecycle + Wallet Service(zkMPC)` 조합으로 본다.
- 개인 사용자용 시나리오는 `Wallet Service(zkMPC)`의 `keygen`과 `sign`을 직접 보여주는 구조로 본다.
- `FS-4`는 공급량/준비금 전체 운영 대시보드로 과장하지 않고 `audit log / 요청 이력 확인` 중심으로 잡는다.

### zkMPC에서 직접 확인된 기능
- `Tenant/User Service`: tenant 등록, 사용자 등록
- `Wallet Service`: wallet 생성, wallet ↔ key 매핑
- `Key Control`: DKG 기반 keygen, sign, recover, refresh, reshare
- `Audit`: tenant_id, wallet_id, key_id, sign_id 중심 감사 이벤트 모델 필요성 확인
- `Object Model`: Tenant → User → Wallet → Key

### 상위 플랫폼 또는 별도 서비스가 필요한 기능
- 수탁 등록 자체의 business workflow
- 입금 감지, 내부 원장 반영, custody ledger 처리
- 출금 승인, 다중 승인 정책, approval group 계산
- 발행 계획 생성, 준비금 검증, mint / burn 비즈니스 로직
- cold wallet 연계, 리밸런싱, 정책 엔진, 최종 tx broadcast

### 데모 표현 원칙
- `zkMPC가 직접 하는 것`은 명시적으로 보여준다.
- `고객 플랫폼이 해야 하는 것`은 화면 설명 안에서 별도 경계로 구분한다.
- `Phase 2~3 또는 운영 고도화` 성격의 항목은 메인 데모가 아니라 옵션/보조 패널로 내린다.
- 사용자나 운영자가 당장 알 필요 없는 내부 저장 구조는 기본 화면 설명에서 숨긴다.

### 로컬 문서 기준 참고 근거
- `SDS Process Diagram`
  - [SDS2025 ScLifecycle 프로세스 다이어그램.pdf](/Users/cho/document-agent/sclifecycle/SDS2025%20ScLifecycle%20프로세스%20다이어그램.pdf)
  - 금융기관용 actor, 발행/준비금, 사용자/포트폴리오, 수탁 등록/수탁 출금 흐름 참조
- `Tenant/User Service`
  - `products/zkmpc/outputs/phase0/product_phase0_review.md`
  - `Tenant/User Service: 테넌트 등록, 사용자 등록`
- `Wallet Service`
  - 같은 문서의 `Wallet 생성`, `Wallet ↔ Key 매핑`
- `Keygen`
  - 같은 문서의 `DKG, ECDSA, 2-of-3`, `Share Vault 저장`
- `Sign`
  - 같은 문서의 `사용자 검증 -> key share 조회 -> partial signature 생성 -> rawSignature 반환`
- `Tenant Audit`
  - `products/zkmpc/compliance/inputs/zkWallet_AuditLog_draft_0410.md`
  - `tenant_id`, `X-Tenant-Id`
- `Signer quorum`
  - `products/zkmpc/.../threshold-ecdsa/process-flow.md`
  - signer subset, presign, sign 단계

### SDS 프로세스 다이어그램에서 직접 반영할 포인트
- 플랫폼 actor는 `사용자 승인/정지`, `준비금/유동성 승인`, `AML`, `수탁사 역할`을 함께 가진다.
- 발행사는 복수로 존재할 수 있고, `발행/소각 계획`과 `준비금 승인 역할`을 가진다.
- 법인 사용자와 개인 사용자는 KYC/승인 상태를 거쳐 지갑 주소를 부여받는다.
- `수탁 등록`이 승인되면 `수탁 건마다 전용 지갑`이 생성된다.
- `수탁 출금`은 `수탁 화이트리스트 주소` 내에서만 가능한 것으로 표현하는 것이 맞다.
- `준비금 유동성 관리`는 `플랫폼과 발행사 간 승인 순서가 무관`하며, `발행사의 출금 계좌가 사전에 등록`되어 있어야 한다.
- `추가 발행/상환`은 SC Lifecycle이 Wallet Service에 `전자서명 요청/반환`을 보내는 구조로 표현할 수 있다.

## 6. 기존 demo_시나리오_usecase_v6.md 연계 기준

- 이 문서는 기존 [demo_시나리오_usecase_v6.md](/Users/cho/document-agent/demo-scenario/demo_시나리오_usecase_v6.md)와 완전히 별개의 문서가 아니라, 그 문서의 `zkMPC` 축을 금융기관용 actor 기준으로 다시 해석한 설계 문서다.
- 다만 `v6`는 `Phase 1 기술 데모 기준선`이고, 이 문서는 `금융기관용 데모 페이지 설계`이므로 1:1 대응이 아니라 `직접 연계`, `부분 연계`, `확장 해석` 3가지로 봐야 한다.
- 특히 `수탁 운영용`과 `스테이블코인 운영용`은 `v6`의 메인 범위가 아니라 `추후 확장 후보` 성격을 일부 끌어온 것이므로, 실제 구현 완료 기능처럼 과장하면 안 된다.

| 현재 spec 항목 | v6 연계 시나리오 | 연계 수준 | 설명 |
|---|---|---|---|
| CU-1. 수탁용 지갑 개설 | 시나리오 A + 운영 콘솔 확장 | 부분 연계 | 내부 `지갑 생성 + keygen`은 시나리오 A와 이어지지만, 수탁 개설 workflow는 금융기관용으로 확장한 설계다. |
| CU-2. 수탁용 입금 | 직접 대응 없음 | 신규 설계 | 주소 발급과 wallet 식별자는 zkMPC와 연결되지만, 입금 감지와 원장 반영은 상위 플랫폼 책임이다. |
| CU-3. 수탁용 출금 | 시나리오 B + 운영 콘솔 확장 | 부분 연계 | 내부 `서명 받아오기`는 시나리오 B와 이어지지만, 요청/승인/브로드캐스트는 운영 플랫폼 계층이다. |
| FU-1. 지갑 생성 -> keygen | 시나리오 A | 직접 연계 | `keygen`, `DKG`, `분산 키 저장 처리`, `Wallet ID / 주소 매핑`이 거의 그대로 이어진다. |
| FU-2. 거래 서명 요청 | 시나리오 B | 직접 연계 | `sign 요청`, `partial signature`, `raw signature 반환`, `최종 제출은 고객 플랫폼 책임` 경계가 그대로 이어진다. |
| PO-1. 플랫폼 운영자 설정 | 직접 대응 없음 | 신규 설계 | `tenant`, `역할 지정`, `권한 부여`는 금융기관용 데모를 위해 추가한 운영 컨텍스트 계층이다. |
| IS-1. 발행사 운영 참여 설정 | 직접 대응 없음 | 신규 설계 | 발행사 actor를 플랫폼 운영자와 분리해서 보여주기 위한 참여/연결 화면이다. |
| FS-2. 발행 요청 | 시나리오 G + 시나리오 B | 확장 해석 | `발행 워크플로`는 v6에서 확장 후보이며, 내부 `Wallet Service 서명 연계`는 시나리오 B 구조를 재사용한다. |
| FS-3. 소각 요청 | 시나리오 G + 시나리오 B | 확장 해석 | `소각 워크플로`는 v6에서 확장 후보이며, 내부 `Wallet Service 서명 연계`는 시나리오 B 구조를 재사용한다. |
| FS-4. 준비금 유동성 관리 요청 | 시나리오 G + 시나리오 B audit | 부분 연계 | `준비금 반영` 자체는 확장 후보이고, 현재 spec은 안전하게 `audit log / 요청 이력 확인` 수준으로만 제한한다. |
| Option O1/O2/O3 | 시나리오 F/G의 운영 콘솔 방향 | 확장 해석 | 관리자 화면, 권한 관리, 고보안 signer 옵션은 `운영 UI/정책/승인` 계층으로서 v6 확장 방향에 가깝다. |

### 해석 원칙
- `개인 사용자용`은 `v6`의 `시나리오 A/B`를 거의 그대로 페이지형으로 옮기는 축이다.
- `수탁 운영용`은 `A/B`의 기술 축을 금융기관 수탁 흐름으로 다시 감싼 구조다.
- `발행사용`은 `v6`에 직접 완성된 스토리가 있다기보다, `시나리오 G`와 `운영 콘솔 확장 방향`을 금융기관용으로 구체화한 축이다.
- 따라서 데모 소개 시에는 `개인 사용자용은 기존 기술 시나리오 기반`, `수탁/발행사/플랫폼 운영용은 금융기관용 확장 설계안`이라고 분명히 구분해서 말하는 것이 맞다.

## 7. 금융기관용 Use Case 맵

| 구분 | Use Case | 대표 actor | 화면 타입 | 관련 시스템 |
|---|---|---|---|---|
| 플랫폼 운영자용 | PO-1 플랫폼 운영자 설정 | 플랫폼 운영자 | 관리자 콘솔 | Tenant/User Service + Wallet Service + Audit |
| 수탁 운영용 | CU-1 수탁용 지갑 개설 | 수탁 운영자, 법인 사용자 | 관리자 콘솔 | Tenant/User Service + Wallet Service + MPC Keygen |
| 수탁 운영용 | CU-2 수탁용 입금 | 수탁 운영자, 법인 사용자 | 관리자 콘솔 | Wallet Service + 상위 플랫폼 입금 감지/원장 |
| 수탁 운영용 | CU-3 수탁용 출금 | 수탁 운영자, 승인자 | 관리자 콘솔 | 상위 플랫폼 승인 + Wallet Service(zkMPC) Sign + Audit |
| 발행사용 | IS-1 발행사 운영 참여 설정 | 발행사 관리자 | 관리자 콘솔 | Tenant/User Service + SC Lifecycle + Audit |
| 발행사용 | FS-2 발행 요청 | 발행사 관리자, 승인자 | 관리자 콘솔 | SC Lifecycle + Wallet Service(zkMPC) |
| 발행사용 | FS-3 소각 요청 | 발행사 관리자, 승인자 | 관리자 콘솔 | SC Lifecycle + Wallet Service(zkMPC) |
| 발행사용 | FS-4 준비금 유동성 관리 요청 | 발행사 관리자, 감사자 | 관리자 콘솔 | SC Lifecycle + Audit Log |
| 개인 사용자용 | FU-1 지갑 생성 -> keygen | 개인 사용자 | 슈퍼앱 화면 | Wallet Service(zkMPC) |
| 개인 사용자용 | FU-2 거래 서명 요청 | 개인 사용자 | 슈퍼앱 화면 | Wallet Service(zkMPC) |

## 8. zkMPC 기능 매핑 메모

아래 표는 현재 로컬 `zkMPC` 문서 기준으로, 어떤 action이 `직접 기능`인지 `상위 플랫폼 필요`인지 구분한 것이다.

| actor | 대단위 시나리오 | 중단위 action | zkMPC 직접 매핑 | 상위 플랫폼 필요 | 비고 |
|---|---|---|---|---|---|
| 개인 사용자 | 계정 생성 | 개인 사용자 등록 | `Tenant/User Service` | KYC / 앱 가입 흐름 | 사용자 등록은 가능하나 금융 가입 절차는 플랫폼 영역 |
| 개인 사용자 | 지갑 생성 | wallet 생성 | `Wallet Service` | 앱 화면 / 계정 연결 | wallet ↔ key 매핑 전 단계 |
| 개인 사용자 | 지갑 생성 | 키 생성 | `DKG / 분산 키 저장 처리` | 없음 | 시나리오 A 직접 연계 |
| 개인 사용자 | 거래 요청 | 거래 내역 입력 | 없음 | 거래 생성 UI / payload 생성 | 서명 전의 intent 생성은 상위 앱 |
| 개인 사용자 | 거래 요청 | 서명 요청 | `Sign / partial signature / raw signature 반환` | tx 조립 / 제출 | 시나리오 B 직접 연계 |
| 개인 사용자 | 거래 요청 | 서명 내역 확인 | `Audit` 확장 가능 | 거래 히스토리 UI | sign_id, wallet_id 중심 노출 |
| 수탁 운영자 | 수탁 개설 | 법인 사용자 등록 | `Tenant/User Service` | 실명확인 / 계약 / 계정개설 | 법인 온보딩 절차는 플랫폼 |
| 수탁 운영자 | 수탁 개설 | 법인 지갑 생성 | `Wallet Service` | 승인 workflow | 지갑 개설 자체는 직접 가능 |
| 수탁 운영자 | 수탁 개설 | 법인 키 생성 | `DKG / Key Create` | 다중 승인 정책 | key ceremony 근거 노출 가능 |
| 수탁 운영자 | 수탁 개설 | 수탁 전용 지갑 생성 | `Wallet / Key 생성 재사용` | 수탁 승인 workflow | SDS 기준 `수탁 건마다 전용 지갑 생성` |
| 수탁 운영자 | 수탁 입금 | 수탁 등록 | 없음 | custody 계약 / 내부 원장 | zkMPC 직접 범위 아님 |
| 수탁 운영자 | 수탁 입금 | 입금 주소 발급 | `Wallet ↔ Address 매핑` | 자산별 주소 정책 | address 노출은 가능하나 감지는 별도 |
| 수탁 운영자 | 수탁 입금 | 입금 감지 및 반영 | 없음 | 체인 모니터 / 내부 원장 반영 | 메인 데모에서는 상태 카드 수준이 안전 |
| 수탁 운영자 | 수탁 출금 | 출금 요청 접수 | 없음 | 출금 신청 / 금액 / 목적지 검증 | 운영 플랫폼 책임 |
| 수탁 운영자 | 수탁 출금 | 출금 승인 | `Audit 필드 모델 참고 가능` | policy / approval group | `approver_id`, `policy_version`은 증빙 모델로 참고 |
| 수탁 운영자 | 수탁 출금 | 화이트리스트 주소 검증 | 없음 | whitelist 정책 / 주소 검증 | SDS 기준 출금 주소는 수탁 화이트리스트 내여야 함 |
| 수탁 운영자 | 수탁 출금 | 서명 받아오기 | `Sign` | unsigned tx 생성 | 시나리오 B 재사용 |
| 수탁 운영자 | 수탁 출금 | 실제 tx 제출 | 없음 | broadcast / 결과 반영 | 최종 제출은 고객 플랫폼 책임 |
| 발행사 관리자 | 가입 | 발행사 등록 / 플랫폼 승인 | `Tenant/User Service` 일부 | 심사 / 권한 승인 | 발행사 business onboarding은 외부 |
| 발행사 관리자 | 발행 | 발행 계획 생성 | 없음 | 발행 정책 / 준비금 검토 | 메인 데모에서는 요청 객체로 표현 |
| 발행사 관리자 | 발행 | mint tx 서명 | `Sign` | mint tx 생성 / 제출 | 시나리오 G의 안전한 축소판 |
| 발행사 관리자 | 소각 | burn 요청 / burn tx 서명 | `Sign` | 소각 정책 / 제출 | 발행과 유사 |
| 발행사 관리자 | 준비금 유동성 | 원화계좌 입출금 요청 | 없음 | 은행 계좌 시스템 / 승인 흐름 | SDS 기준 발행사 출금계좌 사전 등록 필요 |
| 플랫폼 운영자 | 테넌트 가입 | admin 사용자 등록 | `Tenant/User Service` | IAM / 승인 흐름 | tenant context와 role 부여가 핵심 |
| 운영 / 보안 | 재해 복구 | key reshare / recovery | `Recover / Reshare` | 운영 Runbook | 메인 데모보다 옵션 패널이 적절 |
| 운영 / 보안 | 고도 보안 | identifiable abort | 로드맵 성격 | 별도 운영 화면 | Phase 2~3 항목으로 제한 |

## 9. 수탁 운영용 상세 spec

### CU-1. 수탁용 지갑 개설

#### 목적
- 금융기관이 법인 또는 기관 고객의 수탁 계정을 개설할 때, `법인 사용자 등록 -> 지갑 생성 -> 키 생성`이 어떤 순서로 이어지는지 보여준다.
- 이 화면은 `수탁 등록 workflow 전체`를 다 구현했다기보다, 그중 `zkMPC가 실제로 담당하는 wallet / key 영역`을 선명하게 보여주는 데 목적이 있다.

#### 주요 기능
- `법인 사용자 등록`
- `수탁 승인 이후 전용 지갑 개설`
- `수탁 지갑 생성`
- `MPC keygen / DKG`
- `wallet ↔ key 매핑`
- `개설 이력 / audit event 확인`

#### 사용자 화면
- 법인 사용자 기본정보 카드
- 수탁 승인 상태
- 수탁 지갑 개설 버튼
- 지갑 유형 표시
- 키 생성 진행 상태
- wallet id / address / key id 요약
- 최근 개설 이력

#### 화면 설명 / 처리 개요
- 수탁 운영자가 법인 사용자를 등록하거나 기존 사용자를 선택한다.
- 화면에서는 수탁 승인 상태와 지갑 개설 가능 여부를 먼저 보여준다.
- 지갑 개설 요청 후 보안 키 생성이 진행 중임을 단계별 상태로 보여준다.
- 완료 후 지갑 주소, 지갑 ID, 상태가 연결되어 보인다.
- 결과는 이력과 audit 가능한 형태로 남는다.

#### 진행 단계
1. 법인 사용자 선택 또는 등록
2. 수탁 승인 상태 확인
3. 수탁 전용 지갑 생성 요청
4. 보안 키 생성 진행
5. 지갑 연결 완료
6. 개설 결과 / audit 확인

#### 설명
- 이 시나리오에서 `수탁 개설 승인`, `계약`, `실사`는 상위 플랫폼 영역이다.
- SDS 문서 기준으로는 `수탁이 승인될 때 수탁 건마다 전용 지갑이 생성`되는 구조로 읽히므로, 데모도 이 흐름을 따르는 편이 자연스럽다.
- 사용자나 운영자 화면에서는 `분산 보안 처리로 지갑이 개설된다`는 수준만 이해하면 충분하고, `Vault` 같은 내부 저장 구조는 기본 설명에서 숨기는 것이 맞다.
- 메인 데모에서는 `지갑 개설`과 `키 생성`을 연결해 보여주고, 다중 승인이나 cold wallet 연계는 보조 설명으로 내리는 것이 안전하다.

### CU-2. 수탁용 입금

#### 목적
- 수탁 고객에게 입금 경로를 열어주고, 입금 상태가 내부적으로 어떻게 관리되는지 보여준다.
- 다만 `온체인 입금 감지`, `내부 원장 반영`, `수탁 등록`은 zkMPC 단독 기능이 아니므로, 화면에서는 경계를 분명히 보여줘야 한다.

#### 주요 기능
- `수탁 등록 상태 확인`
- `수탁 지갑 / 입금 주소 확인`
- `입금 주소 발급 또는 조회`
- `입금 감지 상태 표시`
- `입금 반영 결과 확인`
- `입금 관련 audit / trace 연결`

#### 사용자 화면
- 법인 고객명 / 수탁 계정 카드
- 수탁 승인 상태 배지
- 입금 주소 카드
- 최근 입금 요청 또는 입금 감지 상태
- 입금 반영 결과
- 관련 request id / trace id

#### 화면 설명 / 처리 개요
- 수탁 운영자가 특정 법인 고객의 수탁 건을 선택한다.
- 시스템이 wallet 기준 입금 주소 또는 계정 정보를 보여준다.
- 상위 플랫폼의 입금 감지 서비스가 온체인 이벤트를 수집한다.
- 감지 결과가 내부 원장 또는 custody ledger에 반영된다.
- 운영자는 결과와 연관된 wallet / audit context를 확인한다.

#### 진행 단계
1. 수탁 건 선택
2. 입금 주소 확인
3. 온체인 입금 감지
4. 내부 원장 반영
5. 입금 이력 확인

#### 설명
- 이 시나리오의 핵심은 `zkMPC가 주소와 wallet identity를 제공`하고, `입금 감지와 반영은 상위 플랫폼이 수행`한다는 경계를 보여주는 것이다.
- 메인 데모에서 `입금 자동화 완성품`처럼 보이게 하기보다, `입금 추적 컨텍스트가 wallet 기준으로 연결된다`는 점을 보여주는 것이 적절하다.

### CU-3. 수탁용 출금

#### 목적
- 수탁 출금에서는 `요청 -> 승인 -> 서명 받아오기 -> 실제 tx 제출 -> 이력 확인` 흐름이 어떻게 나뉘는지 보여준다.
- 이 중 `서명 받아오기`가 zkMPC의 직접 가치이고, 그 앞뒤의 승인 및 제출은 플랫폼 계층이라는 점을 명확히 한다.

#### 주요 기능
- `출금 요청 접수`
- `승인 / policy 검토`
- `수탁 화이트리스트 주소 검증`
- `Wallet Service 서명 요청`
- `raw signature 또는 서명 결과 반환`
- `출금 결과 / audit 확인`

#### 사용자 화면
- 출금 요청 리스트
- 요청 상세: 목적지, 금액, 자산, 사유
- 승인 상태 / approval group 상태
- 화이트리스트 주소 검증 상태
- 서명 진행 상태
- tx 결과 / audit event / sign id

#### 화면 설명 / 처리 개요
- 법인 사용자 또는 운영자가 출금 요청을 생성한다.
- 상위 플랫폼이 정책과 승인 요건을 검토한다.
- 목적지 주소가 수탁 화이트리스트 내에 있는지 검증한다.
- 승인 완료 후 unsigned tx 또는 sign payload가 Wallet Service로 전달된다.
- Wallet Service가 서명 세션을 만들고 raw signature를 반환한다.
- 상위 플랫폼이 최종 tx를 제출하고 결과를 기록한다.
- 감사자는 sign id, wallet id, request id를 기준으로 이력을 조회한다.

#### 진행 단계
1. 출금 요청 생성
2. 승인 / policy 검토
3. 화이트리스트 주소 검증
4. Wallet Service 서명 요청
5. 실제 tx 제출
6. 출금 이력 확인

#### 설명
- `출금 승인`은 zkMPC 핵심 기능이 아니라 운영 / 정책 계층이다.
- SDS 문서 기준으로 `출금 주소가 수탁 화이트리스트 내에 있어야 함`을 명시하는 것이 맞다.
- 다만 감사 모델에서는 `approver_id`, `approval_group`, `policy_version`, `tx_hash` 같은 필드를 같이 보여주면 설득력이 커진다.

## 10. 플랫폼 운영자용 상세 spec

### PO-1. 플랫폼 운영자 설정

#### 목적
- tenant를 만들고, 플랫폼 운영자가 발행사와 수탁 운영 주체를 연결할 수 있는 초기 설정 화면이 필요하다.
- 이 단계에서 이후 수탁/발행/소각 요청이 어느 tenant 아래에서 실행되는지 보여줘야 한다.

#### 주요 기능
- `tenant 할당`
- `프로그램 생성`
- `플랫폼 운영 권한 설정`
- `발행사 관리자 지정`
- `수탁 운영자 지정`
- `운영 권한 / 승인 권한 분리`
- `Wallet Service 연계 준비 상태 확인`

#### 사용자 화면
- tenant 생성 또는 선택
- 프로그램명 입력
- 플랫폼 운영 권한 설정
- 발행사 관리자 지정
- 수탁 운영자 지정
- 승인권자 / 준법감시인 지정
- 생성 완료 후 프로그램 요약 카드

#### 화면 설명 / 처리 개요
- 플랫폼 운영자가 tenant를 생성하거나 기존 tenant를 선택한다.
- 이 화면에서는 어떤 actor가 어느 역할로 들어오는지 먼저 정리한다.
- 발행사 관리자, 수탁 운영자, 승인자 역할을 연결하고 운영 범위를 설정한다.
- 완료 후 `이 tenant에서 어떤 조직이 어떤 기능을 담당하는지` 한눈에 보이는 요약 상태를 제공한다.

#### 진행 단계
1. tenant 생성 또는 선택
2. 프로그램 생성
3. 플랫폼 운영 권한 설정
4. 발행사 / 수탁 운영자 지정
5. 승인권자 지정
6. Wallet Service 연계 준비 완료

#### 설명
- 실제 수탁/발행 이전에 `누가 어떤 프로그램을 운영하는가`를 먼저 확정하는 화면이다.
- 이후 수탁/발행/소각 요청은 모두 이 tenant와 프로그램 컨텍스트 아래에서 수행된다.
- 이 화면은 `플랫폼 운영자 관점` 화면이고, 발행사 관리자가 보는 화면과는 분리해서 다루는 것이 맞다.

## 11. 발행사용 상세 spec

### IS-1. 발행사 운영 참여 설정

#### 목적
- 발행사 관리자가 자기 조직이 어떤 스테이블코인 프로그램에 참여하고, 어떤 권한과 지갑 상태를 갖는지 확인하는 별도 화면이 필요하다.
- 플랫폼 운영자 화면과 섞지 않고, `발행사 입장`에서 필요한 정보만 보여주는 것이 목적이다.

#### 주요 기능
- `발행사 기본 정보 확인`
- `참여 프로그램 확인`
- `발행 / 소각 권한 확인`
- `발행사 지갑 상태 확인`
- `승인권자 / 준비금 역할 확인`

#### 사용자 화면
- 발행사 정보 카드
- 참여 중인 프로그램 목록
- 발행 / 소각 가능 상태
- 발행사 지갑 상태 카드
- 승인권자 / 준비금 담당자 표시

#### 화면 설명 / 처리 개요
- 발행사 관리자는 자신의 조직이 어떤 프로그램에 연결되어 있는지 먼저 확인한다.
- 화면에서는 발행 / 소각 / 준비금 관련 역할이 어떤 상태인지 요약해서 보여준다.
- 이후 발행 요청이나 소각 요청 화면으로 이동할 수 있도록 진입점을 제공한다.

#### 진행 단계
1. 참여 프로그램 확인
2. 권한 상태 확인
3. 발행사 지갑 상태 확인
4. 승인자 / 준비금 역할 확인
5. 후속 운영 화면 진입

#### 설명
- 이 화면은 `발행사 관리자 관점`의 시작 화면이다.
- 같은 관리자 콘솔이라도 플랫폼 운영자 화면과 발행사 화면은 actor가 다르므로 분리하는 것이 기획상 더 자연스럽다.

### FS-2. 발행 요청

#### 목적
- 발행 요청을 생성하고 승인자 검토를 거쳐 Wallet Service 서명 연계까지 이어지는 운영 흐름을 보여준다.

#### 주요 기능
- `발행 요청 생성`
- `발행 사유 / 근거 입력`
- `준비금 내역 등록`
- `승인 요청`
- `Wallet Service(zkMPC) 서명 연계`
- `발행 실행 결과 기록`

#### 사용자 화면
- 발행 수량 입력
- 발행 사유 / 근거 문서 요약
- 준비금 내역 카드
- 요청 생성 버튼
- 승인 대기 상태
- 승인 완료 / 서명 연계 상태
- request id / audit event id

#### 화면 설명 / 처리 개요
- 발행사 관리자가 발행 요청을 생성한다.
- 시스템이 tenant/program 기준으로 요청을 기록한다.
- 준비금 내역이 먼저 등록되고 준비금 승인 상태가 연결된다.
- 승인자에게 발행 계획 검토 요청이 전달된다.
- 승인 완료 시 SC Lifecycle이 Wallet Service에 전자서명 요청을 보낸다.
- Wallet Service는 서명 결과를 반환하고, 시스템은 발행 결과를 기록한다.

#### 진행 단계
1. 발행 요청 생성
2. 준비금 내역 등록
3. 발행 계획 승인 대기
4. Wallet Service 서명 요청
5. 발행 실행 / 결과 기록

#### 설명
- `발행 버튼`보다 `요청 -> 승인 -> 서명 연계 -> 결과 기록`의 통제 흐름이 핵심이다.
- SDS 문서 기준으로는 `준비금 내역 등록 -> 준비금 승인 -> 발행 계획 승인 -> 발행 실행`의 흐름이 전제된다.
- 실제 서명 내부는 상세 기술 패널로 연결하고, 메인 화면은 운영자 관점에 집중한다.

### FS-3. 소각 요청

#### 목적
- 발행과 대칭되는 소각 흐름도 요청, 승인, 결과 기록 체계 안에 있다는 점을 보여준다.

#### 주요 기능
- `소각 요청 생성`
- `대상 수량 / 대상 계정 확인`
- `승인 요청`
- `Wallet Service(zkMPC) 서명 연계`
- `소각 결과 기록`

#### 사용자 화면
- 소각 수량 입력
- 대상 계정 / 이유 확인
- 승인 상태
- 소각 완료 상태
- 발행/소각 이력 비교

#### 화면 설명 / 처리 개요
- 발행사 관리자가 소각 요청을 생성한다.
- 시스템이 대상 계정과 수량을 검증한다.
- 승인자가 요청을 승인한다.
- SC Lifecycle이 Wallet Service에 서명 요청을 보낸다.
- 실행 결과와 audit trail이 저장된다.

#### 진행 단계
1. 소각 요청 생성
2. 대상 수량 확인
3. 승인 대기
4. Wallet Service 서명 요청
5. 결과 기록

#### 설명
- 발행과 소각을 같은 운영 통제 모델로 보여주되, 요청 사유와 결과 추적이 분리되어야 한다.

### FS-4. 준비금 유동성 관리 요청

#### 목적
- 준비금/유동성 전체 관제를 과장하지 않고, 관련 요청이 audit 가능한 형태로 남는다는 점을 보여준다.

#### 주요 기능
- `준비금 / 유동성 관리 요청 생성`
- `요청 사유 기록`
- `관련 관리자 액션 연계`
- `audit log 확인`
- `요청 결과 이력 확인`

#### 사용자 화면
- 유동성 관리 요청 생성
- 요청 유형 선택
- request id
- audit log 목록
- 요청 상태 타임라인

#### 화면 설명 / 처리 개요
- 관리자가 준비금 또는 유동성 관련 요청을 생성한다.
- 시스템이 요청 맥락과 관리자 액션을 기록한다.
- 발행사 승인과 플랫폼 승인 상태를 각각 기록한다.
- 승인 완료 후 원화계좌 입출금 요청이 처리되고 반영 결과가 저장된다.
- 감사자는 audit log와 요청 이력만으로 무엇이 일어났는지 확인한다.

#### 진행 단계
1. 요청 생성
2. 사유 기록
3. 발행사 / 플랫폼 승인
4. 원화계좌 입출금 처리
5. audit log 기록
6. 이력 확인

#### 설명
- `공급량/준비금 대시보드 전체`보다 `audit log 확인` 중심으로 가는 것이 맞다.
- SDS 문서 기준으로 `플랫폼 및 발행사 간 승인 순서는 관계없음`, `발행사의 출금 계좌는 사전 등록` 조건을 보조 설명으로 붙이는 것이 자연스럽다.
- 운영 요청이 감사 가능한 형태로 남는 구조를 보여주는 데 집중한다.

## 12. 개인 사용자용 상세 spec

### FU-1. 지갑 생성 -> keygen

#### 목적
- 개인 사용자가 슈퍼앱 안에서 지갑을 생성할 때, 내부적으로 keygen이 일어난다는 점을 보여준다.

#### 주요 기능
- `지갑 생성 요청`
- `보안 키 생성 시작`
- `생성 진행 상태 표시`
- `지갑 연결 완료`
- `Wallet ID / 주소 매핑`

#### 사용자 화면
- 지갑 생성 CTA
- 생성 진행 상태
- 완료 후 잔액 / 주소 / wallet id
- "개인키는 단일 서버에 저장되지 않음" 안내

#### 화면 설명 / 처리 개요
- 개인 사용자가 지갑 생성을 요청한다.
- 앱에서는 지갑 생성이 진행 중이라는 상태를 보여준다.
- 내부적으로는 분산 보안 처리로 키가 준비되지만, 사용자는 이를 상세히 알 필요는 없다.
- 완료 후 wallet id와 주소가 사용자 화면에 연결된다.

#### 진행 단계
1. 지갑 생성 요청
2. 보안 키 생성 시작
3. 생성 진행 상태 확인
4. 지갑 연결 완료
5. 주소 매핑 완료

#### 설명
- 메인 화면은 앱형 UX로 보여주고, 중앙 패널에서는 `안전하게 지갑이 생성되는 과정` 정도만 설명하는 것이 적절하다.
- `Vault` 같은 내부 저장 구조는 사용자 설명에 직접 넣지 않는 편이 낫다.

### FU-2. 거래 서명 요청

#### 목적
- 개인 사용자가 TX를 요청했을 때 Wallet Service가 서명을 어떻게 가져오는지 보여준다.

#### 주요 기능
- `거래 요청 생성`
- `사용자 검증`
- `signer subset / quorum 구성`
- `partial signature 생성`
- `raw signature 반환`

#### 사용자 화면
- 거래 요청 버튼
- 서명 요청 진행 상태
- 서명 완료 상태
- tx 요청 요약

#### 화면 설명 / 처리 개요
- 개인 사용자가 거래 요청을 시작한다.
- 시스템이 사용자 검증을 수행한다.
- Wallet Service가 signer subset을 구성한다.
- partial signature를 수집하고 최종 서명 결과를 조합한다.
- 상위 플랫폼은 이를 받아 후속 제출을 수행한다.

#### 진행 단계
1. 거래 요청 생성
2. 사용자 검증
3. signer quorum 구성
4. partial signature 생성
5. raw signature 반환

#### 설명
- `개인이 거래를 요청했을 때 내부적으로 어떻게 서명이 조합되는지`를 보여주는 데 초점을 둔다.
- 최종 제출은 상위 플랫폼 책임이라는 경계를 문구로 분명히 둔다.

## 13. 내부 옵션

### Option O1. 플랫폼 운영자 역할 관리 화면
- PO-1 보강 기능
- 보여줄 것:
  - 관리자 목록
  - 역할 변경
  - 권한 범위
  - 최근 관리자 액션 로그

### Option O2. 발행자 지갑 / 관리자 지갑 카드
- FS-2, FS-3 보강 기능
- 보여줄 것:
  - 발행자 지갑 카드
  - 관리자 지갑 카드
  - 권한별 액션 버튼
  - 관리자 지갑 audit log

### Option O3. 고보안 배포 / HSM signer
- IT / 보안팀용 보조 패널
- 보여줄 것:
  - Private Cloud
  - HSM signer 등록 여부
  - signer 상태 모니터링

### Option O4. Cold / Hot Wallet 상태 카드
- 수탁 운영용 보강 기능
- 보여줄 것:
  - hot / cold 구분
  - 현재 운영 상태
  - 내부 이동 요청 상태
  - 월별 snapshot 요약

### Option O5. Key Refresh / Reshare 패널
- 운영 / 보안팀용 보조 패널
- 보여줄 것:
  - reshare 요청 버튼
  - old / new participant set 비교
  - rotation 사유
  - 완료 / 실패 audit 이벤트

## 14. 현재 결론
- 금융기관용은 `플랫폼 운영자용`, `수탁 운영용`, `발행사용`, `개인 사용자용` 네 축으로 보는 것이 맞다.
- `zkMPC`가 직접 보여줄 핵심은 `tenant / user / wallet / key`, `keygen`, `sign`, `audit context`다.
- 수탁 운영용에서는 `수탁용 지갑 개설`, `수탁용 입금`, `수탁용 출금`이 핵심이고, 이 중 `입금 감지`, `승인`, `브로드캐스트`는 상위 플랫폼 책임으로 분리해 보여줘야 한다.
- 발행사용에서는 `발행사 참여 상태 확인`, `발행`, `소각`, `준비금 관련 이력 확인`이 핵심이다.
- 플랫폼 운영자용에서는 `tenant 생성`, `역할 지정`, `발행사/수탁사 연결`이 핵심이다.
- 개인 사용자용에서는 `지갑 생성(keygen)`, `거래 서명 요청`이 핵심이다.
- 페이지 프레임은 공통으로 `사용자 화면 / 화면 설명 / 진행 단계 / 설명` 4분할을 유지하는 것이 가장 적절하다.
