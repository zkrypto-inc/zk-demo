# zkWallet Demo Impl MD v1

## 목적
- 이 문서는 [zkwallet_spec_finanical.md](/Users/cho/document-agent/demo-scenario/design/zkwallet_spec_finanical.md)를 실제 데모 페이지 단위로 내리는 구현 문서다.
- 초점은 `기능 설명`보다 `step별 화면이 어떻게 보여야 하는가`에 둔다.
- 각 시나리오는 `진행 단계`를 기준으로 나누고, 각 단계마다 `사용자 화면`, `중앙 패널`, `진행 단계`, `설명`을 정의한다.

## 문서 사용 기준
- `spec` 문서는 어떤 Use Case를 보여줄지 정하는 문서다.
- 이 문서는 그 Use Case를 실제 데모 화면으로 어떻게 구현할지를 정하는 문서다.
- 중앙 패널은 꼭 기술 다이어그램일 필요는 없다.
- 중앙 패널은 `이 화면에서 무슨 일이 진행 중인지`를 이해시키는 역할이면 충분하다.
- `Vault`, `party node`, `auxinfo` 같은 내부 용어는 기본 구현 문구에서 숨기고, 필요하면 기술 근거 패널로만 노출한다.

## actor별 화면 타입 기준
| actor | 기본 화면 타입 | 이유 |
|---|---|---|
| 개인 사용자 | `모바일 앱 / 슈퍼앱 화면` | 가입, 지갑 생성, 거래 승인 같은 리테일 경험이 핵심이다. |
| 플랫폼 운영자 | `웹 관리자 콘솔` | tenant, 권한, 감사, 장애 대응처럼 운영 정보가 많다. |
| 발행사 관리자 | `웹 관리자 콘솔` | 발행, 소각, 준비금, 승인 상태를 테이블과 카드로 보는 편이 자연스럽다. |
| 수탁 운영자 | `웹 관리자 콘솔` | 수탁 개설, 입출금, 화이트리스트, 감사 이력은 웹 기반 운영 화면이 적합하다. |
| 수탁 관리자 | `웹 포털 / 웹 관리자 콘솔` | 수탁 승인, 입금/출금 승인, 지갑 운영 참여가 중심이다. |
| 법인 사용자 | `웹 포털` | 수탁 신청, 현황 조회, 요청 이력 중심으로 보여주는 편이 안정적이다. |

- 현재 데모 기준으로는 `개인 사용자만 앱`, `나머지 actor는 웹`으로 고정하는 것이 가장 이해가 쉽다.
- 특히 `수탁`은 모바일보다 `웹 포털 / 웹 콘솔`이 맞다.
- 나중에 법인 사용자 전용 앱을 상정할 수는 있지만, 지금 단계의 데모에서는 우선순위가 낮다.

## 공통 화면 프레임
- 상단:
  - `제품명 / 데모명`
  - `시나리오 선택 dropdown`
  - `현재 actor 배지`
- 좌측:
  - `사용자 화면`
  - 모바일 또는 웹 콘솔 mock
- 중앙:
  - `화면 설명 / 간단한 처리 개요`
  - 필요 시 단순 박스형 흐름도 또는 상태 카드
- 우측 상단:
  - `진행 단계`
  - step n / total 표시
- 우측 하단:
  - `설명`
  - 현재 단계에서 사용자가 이해해야 할 핵심 문장

## 공통 구현 규칙
- `사용자 화면`은 실제 클릭 가능한 화면처럼 보여야 한다.
- `중앙 패널`은 내부 시스템 전체를 다 보여주기보다 현재 단계의 핵심 관계만 보여준다.
- `진행 단계`는 현재 step만 강조하고 나머지는 흐리게 처리한다.
- `설명`은 2~3문장 이내로 유지하고, 정책/경계/제약을 짧게 적는다.
- 각 시나리오는 가능하면 첫 step에서 `요청 시작`, 마지막 step에서 `결과 확인`이 되도록 맞춘다.
- `업무 승인`과 `MPC 서명`은 같은 것으로 보이면 안 된다.
- 발행 / 소각 / 수탁 출금처럼 민감한 요청은 `Approval Service의 2인 승인` 화면이 먼저 보이고, 그 다음에 `Wallet Service 서명 요청` 화면으로 넘어가야 한다.
- 따라서 승인 단계 화면에서는 `1차 승인자`, `2차 승인자`, `approval group`, `현재 대기 상태`가 명확히 보여야 한다.

---

## PO-1. 플랫폼 운영자 설정
- actor: `플랫폼 운영자`
- 화면 타입: `웹 관리자 콘솔`
- total steps: `6`

### Step 1. tenant 생성 또는 선택
- 사용자 화면:
  - tenant 목록
  - `새 tenant 생성` 버튼
  - 최근 생성 tenant 카드
- 중앙 패널:
  - `새 금융기관 프로그램을 시작하기 위해 운영 단위를 선택합니다.`
  - `기존 tenant 재사용` vs `신규 tenant 생성` 두 경로를 보여준다.
- 진행 단계:
  - `1 / 6`
  - `tenant 생성 또는 선택` 활성
- 설명:
  - 모든 발행사, 수탁 운영자, 승인자 연결은 이 tenant를 기준으로 이어진다.

### Step 2. 프로그램 생성
- 사용자 화면:
  - 프로그램명 입력
  - 네트워크 / 자산 유형 선택
  - 저장 버튼
- 중앙 패널:
  - `tenant 아래에서 어떤 프로그램을 운영할지 정의합니다.`
  - 프로그램은 이후 수탁/발행 시나리오의 상위 컨텍스트가 된다.
- 진행 단계:
  - `2 / 6`
  - `프로그램 생성` 활성
- 설명:
  - 이 단계는 실제 사용자 자산 처리 이전의 운영 구조 설정 단계다.

### Step 3. 플랫폼 운영 권한 설정
- 사용자 화면:
  - 운영 역할 목록
  - 권한 토글
  - 운영자 계정 연결 상태
- 중앙 패널:
  - `플랫폼 운영자가 어떤 범위까지 관리할지 설정합니다.`
  - tenant 관리 / 역할 관리 / 감사 보기 같은 권한을 구분한다.
- 진행 단계:
  - `3 / 6`
  - `플랫폼 운영 권한 설정` 활성
- 설명:
  - 플랫폼 운영자 화면은 발행사 화면과 다르므로, 여기서는 운영 범위를 먼저 확정해야 한다.

### Step 4. 발행사 / 수탁 운영자 지정
- 사용자 화면:
  - 발행사 연결 카드
  - 수탁 운영자 연결 카드
  - 현재 연결 상태 배지
- 중앙 패널:
  - `외부 조직과 역할을 tenant에 연결합니다.`
  - 발행사와 수탁 운영자를 각각 별도 actor로 보여준다.
- 진행 단계:
  - `4 / 6`
  - `발행사 / 수탁 운영자 지정` 활성
- 설명:
  - 같은 시스템을 쓰더라도 발행사와 수탁 운영자는 보는 화면과 책임이 다르다.

### Step 5. 승인권자 지정
- 사용자 화면:
  - 승인권자 목록
  - 준법감시인 지정
  - 다중 승인 여부
- 중앙 패널:
  - `누가 승인 체계에 참여하는지 설정합니다.`
  - 이후 수탁 출금, 발행, 소각 요청에서 이 구성이 재사용된다.
- 진행 단계:
  - `5 / 6`
  - `승인권자 지정` 활성
- 설명:
  - 운영자는 승인 구조를 만들고, 실제 승인 실행은 각 actor 시나리오에서 보여준다.

### Step 6. 연계 준비 완료
- 사용자 화면:
  - 프로그램 요약 카드
  - actor 연결 상태
  - 연계 준비 완료 배지
- 중앙 패널:
  - `이 tenant에서 어떤 조직이 어떤 역할로 운영되는지`를 요약한다.
  - 다음으로 갈 수 있는 발행사 / 수탁 운영 시나리오 진입 버튼을 둔다.
- 진행 단계:
  - `6 / 6`
  - `연계 준비 완료` 활성
- 설명:
  - 이 화면은 플랫폼 운영자 시나리오의 종료 화면이며, 이후 각 actor 전용 시나리오로 분기된다.

---

## CU-1. 수탁 등록 및 지갑 개설
- actor: `법인 사용자 / 수탁 운영자`
- 화면 타입: `웹 포털 / 웹 관리자 콘솔`
- total steps: `5`

### Step 1. 법인 사용자 등록 및 수탁 관리자 지정
- 사용자 화면:
  - 법인 정보 입력 폼
  - 수탁 관리자 목록
  - `관리자 추가` 버튼
  - 관리자별 역할 배지
- 중앙 패널:
  - `법인 사용자가 수탁 등록을 요청하고 수탁 관리자 n명을 지정합니다.`
  - 아직 지갑 생성 전이며, 조직과 관리자 구성을 먼저 완료하는 단계라는 점을 보여준다.
- 진행 단계:
  - `1 / 5`
  - `법인 사용자 등록 및 수탁 관리자 지정` 활성
- 설명:
  - 수탁은 단순 계정 개설보다 `누가 지갑 운영에 참여하는가`를 먼저 정하는 구조로 보이는 것이 자연스럽다.

### Step 2. 플랫폼 수탁 승인
- 사용자 화면:
  - 등록 요청 상태 카드
  - 플랫폼 승인 대기 배지
  - 승인 완료 또는 반려 상태
- 중앙 패널:
  - `플랫폼이 수탁 등록 요청을 검토하고 승인합니다.`
  - 승인 완료 후에만 수탁 전용 지갑 생성 요청이 열리도록 보여준다.
- 진행 단계:
  - `2 / 5`
  - `플랫폼 수탁 승인` 활성
- 설명:
  - 이 단계는 수탁 등록과 지갑 개설 사이의 운영 통제 지점을 보여주는 역할을 한다.

### Step 3. 수탁 전용 지갑 생성 요청
- 사용자 화면:
  - 승인 완료 상태
  - 수탁 지갑 유형 표시
  - `수탁 지갑 생성` CTA
  - request id
- 중앙 패널:
  - `플랫폼 승인 이후 수탁 전용 지갑 생성을 요청합니다.`
  - 등록 시점에 전용 지갑을 함께 개설한다는 구조를 설명한다.
- 진행 단계:
  - `3 / 5`
  - `수탁 전용 지갑 생성 요청` 활성
- 설명:
  - 이 단계에서 `등록 시 지갑 개설` 구조가 명확히 보여야 한다.

### Step 4. keygen
- 사용자 화면:
  - keygen 진행 상태
  - 보안 처리 진행 배지
  - 완료 예상 상태
- 중앙 패널:
  - `Wallet Service가 MPC 기반 keygen을 수행합니다.`
  - 내부 프로토콜 전체보다 `수탁 전용 지갑이 안전하게 준비되는 중`이라는 인식이 중요하다.
- 진행 단계:
  - `4 / 5`
  - `keygen` 활성
- 설명:
  - 이 단계가 수탁 시나리오 안에서 zkMPC의 핵심 가치를 직접 보여주는 구간이다.

### Step 5. 수탁 지갑 생성 완료 / 개설 연결 완료
- 사용자 화면:
  - custody wallet 완료 카드
  - wallet id / address / 상태
  - 관리자 연결 상태
  - `수탁 입금`, `수탁 출금` 진입 CTA
- 중앙 패널:
  - `수탁 지갑이 개설되었고 이후 운영 시나리오로 연결됩니다.`
  - 등록 완료와 지갑 준비 완료가 함께 끝난 상태라는 점을 보여준다.
- 진행 단계:
  - `5 / 5`
  - `수탁 지갑 생성 완료 / 개설 연결 완료` 활성
- 설명:
  - 이 화면은 수탁 온보딩의 종료 화면이자 이후 입금/출금 시나리오의 출발점이다.

---

## CU-2. 수탁용 입금
- actor: `법인 사용자 / 수탁 관리자`
- 화면 타입: `웹 포털 / 웹 관리자 콘솔`
- total steps: `4`

### Step 1. 수탁 내역 등록
- 사용자 화면:
  - 입금 자산 선택
  - 입금 수량 입력
  - 메모 / 사유 입력
  - 등록 버튼
- 중앙 패널:
  - `어떤 자산을 어떤 수량으로 수탁 입금할지 등록합니다.`
  - 이 문서 기준에서는 수탁 입금을 실제 온체인 전송형 요청으로 본다는 점을 같이 보여준다.
- 진행 단계:
  - `1 / 4`
  - `수탁 내역 등록` 활성
- 설명:
  - 단순 주소 조회가 아니라 실제 입금 요청을 만드는 단계로 보이게 해야 한다.

### Step 2. 수탁 관리자 t명 승인
- 사용자 화면:
  - Approval Service 상태 카드
  - 수탁 관리자 승인 현황
  - approval group 진행 상태
- 중앙 패널:
  - `수탁 관리자 t명이 입금 요청을 승인합니다.`
  - 승인 완료 후에만 Wallet Service 서명과 tx 전송 단계가 열린다.
- 진행 단계:
  - `2 / 4`
  - `수탁 관리자 t명 승인` 활성
- 설명:
  - 이 단계는 업무 승인과 MPC 서명을 분리해 보여주는 핵심 화면이다.

### Step 3. 실제 서명 생성 / tx 전송
- 사용자 화면:
  - approval complete 상태
  - 서명 진행 상태
  - tx 전송 상태
  - tx hash / request id
- 중앙 패널:
  - `승인 완료 후 Wallet Service가 서명을 생성하고, 상위 플랫폼이 실제 온체인 tx를 전송합니다.`
  - 수탁 입금이 실제 온체인 전송이라는 점을 짧고 분명하게 설명한다.
- 진행 단계:
  - `3 / 4`
  - `실제 서명 생성 / tx 전송` 활성
- 설명:
  - zkMPC의 직접 가치는 `서명 생성`이고, 실제 broadcast는 상위 플랫폼 책임이라는 경계를 유지한다.

### Step 4. 입금 이력 확인
- 사용자 화면:
  - 입금 완료 상태
  - tx hash
  - request id / audit event id
  - 최근 입금 이력 리스트
- 중앙 패널:
  - `입금 결과와 이력이 남아 있는 상태를 보여줍니다.`
  - 이후 감사나 운영 조회 시 어떤 기준으로 추적하는지 같이 설명한다.
- 진행 단계:
  - `4 / 4`
  - `입금 이력 확인` 활성
- 설명:
  - 이 화면은 수탁 입금이 실제로 실행되었고 추적 가능하다는 점을 보여주는 종료 화면이다.

---

## IS-1. 발행사 등록 및 지갑 생성
- actor: `발행사 관리자`
- 화면 타입: `웹 관리자 콘솔`
- total steps: `5`

### Step 1. 발행사 등록
- 사용자 화면:
  - 발행사명 입력
  - 사업자 정보 입력
  - 담당자 정보 입력
  - 등록 요청 버튼
- 중앙 패널:
  - `발행사가 플랫폼에 등록을 요청합니다.`
  - 아직 지갑 생성 전이며, 조직 단위 온보딩을 시작하는 단계라는 점을 보여준다.
- 진행 단계:
  - `1 / 5`
  - `발행사 등록` 활성
- 설명:
  - 발행사 시나리오는 참여 상태 확인보다 먼저 `조직 등록`이 선행되는 것이 자연스럽다.

### Step 2. 플랫폼 승인
- 사용자 화면:
  - 등록 요청 상태 카드
  - 플랫폼 승인 대기 배지
  - 승인 완료 또는 반려 상태
- 중앙 패널:
  - `플랫폼 운영자가 발행사 등록 요청을 검토합니다.`
  - 승인 완료 후에만 발행사 측 지갑 관리자 설정과 지갑 생성이 가능해진다.
- 진행 단계:
  - `2 / 5`
  - `플랫폼 승인` 활성
- 설명:
  - 이 단계는 발행사 입장에서 `플랫폼 승인 결과를 기다리는 상태`를 보여주는 것이 핵심이다.

### Step 3. 지갑 관리자 설정
- 사용자 화면:
  - 관리자 목록
  - `관리자 추가` 버튼
  - 관리자별 역할 선택
  - n명 구성 상태 표시
- 중앙 패널:
  - `발행사 측 지갑 관리자 구성을 설정합니다.`
  - 누가 지갑 생성 이후 운영과 승인에 참여하는지 정하는 단계라는 점을 보여준다.
- 진행 단계:
  - `3 / 5`
  - `지갑 관리자 설정` 활성
- 설명:
  - 여기서는 `n명 지갑 관리자`와 역할 구성이 핵심이며, 이후 발행/소각 승인 흐름의 기반이 된다.

### Step 4. 발행사 지갑 생성
- 사용자 화면:
  - 관리자 구성 요약
  - `지갑 생성` CTA
  - 생성 진행 상태
  - keygen 진행 배지
- 중앙 패널:
  - `발행사 지갑 생성을 시작하면 Wallet Service의 MPC 과정이 진행됩니다.`
  - 내부 구현 전체보다 `여러 관리자를 전제로 지갑이 안전하게 생성된다`는 인식이 중요하다.
- 진행 단계:
  - `4 / 5`
  - `발행사 지갑 생성` 활성
- 설명:
  - 이 단계가 기존 `시나리오 A`의 운영 콘솔 확장에 해당하는 부분이다.

### Step 5. 발행사 지갑 생성 완료
- 사용자 화면:
  - 발행사 지갑 완료 카드
  - wallet id / 상태
  - 관리자 연결 상태
  - `발행 요청`, `소각 요청` 진입 CTA
- 중앙 패널:
  - `발행사 지갑이 준비되었고 후속 운영 시나리오로 이동할 수 있습니다.`
  - 이후 발행/소각/준비금 시나리오가 이 지갑을 기준으로 이어진다는 점을 보여준다.
- 진행 단계:
  - `5 / 5`
  - `발행사 지갑 생성 완료` 활성
- 설명:
  - 이 화면은 발행사 온보딩의 종료 화면이자, 실제 운영 시나리오로 넘어가는 분기점 역할을 한다.

---

## FS-2. 발행 요청
- actor: `발행사 관리자`
- 화면 타입: `웹 관리자 콘솔`
- total steps: `6`

### Step 1. 발행 요청 생성
- 사용자 화면:
  - 발행 수량 입력
  - 발행일자 입력
  - 메모 / 사유 입력
- 중앙 패널:
  - `어떤 발행을 요청하는지 정의합니다.`
  - 아직 승인 전이며, 요청 객체를 만드는 단계라는 점을 보여준다.
- 진행 단계:
  - `1 / 6`
  - `발행 요청 생성` 활성
- 설명:
  - 사용자는 이 단계에서 발행 의도를 만들고 제출 준비를 한다.

### Step 2. 준비금 내역 등록
- 사용자 화면:
  - 준비금 내역 카드
  - 증빙 파일 요약
  - 연결 상태 배지
- 중앙 패널:
  - `발행 요청과 준비금 내역이 연결됩니다.`
  - 금액과 증빙이 일치하는지 확인하는 단계로 표현한다.
- 진행 단계:
  - `2 / 6`
  - `준비금 내역 등록` 활성
- 설명:
  - SDS 기준 발행 흐름에서는 준비금 관련 정보가 먼저 연결되는 형태로 이해시키는 것이 자연스럽다.

### Step 3. 1차 승인
- 사용자 화면:
  - Approval Service 상태 카드
  - 1차 승인자 카드
  - 2차 승인자 대기 상태
- 중앙 패널:
  - `요청이 Approval Service로 전달되고 첫 번째 승인자 검토가 시작됩니다.`
  - 아직 Wallet Service 서명 단계로는 넘어가지 않았음을 분명히 보여준다.
- 진행 단계:
  - `3 / 6`
  - `1차 승인` 활성
- 설명:
  - 이 단계의 핵심은 `업무 승인`이 먼저 진행된다는 점을 보여주는 것이다.

### Step 4. 2차 승인
- 사용자 화면:
  - 1차 승인 완료 배지
  - 2차 승인자 카드
  - approval group 완료 조건
- 중앙 패널:
  - `두 번째 승인자까지 승인해야 서명 요청이 열립니다.`
  - 준법 또는 리스크 관점의 최종 확인 단계라는 느낌을 준다.
- 진행 단계:
  - `4 / 6`
  - `2차 승인` 활성
- 설명:
  - 여기서 `두 명의 승인`이 모두 끝나야 다음 단계의 Wallet Service 서명 요청으로 넘어간다.

### Step 5. Wallet Service 서명 요청
- 사용자 화면:
  - approval complete 상태
  - 서명 진행 상태
  - 요청 ID / 이벤트 ID
- 중앙 패널:
  - `승인 완료 후 발행 실행을 위한 보안 서명 단계가 진행됩니다.`
  - 내부 기술 처리 전체보다 `승인 이후 실행 단계`라는 인식이 중요하다.
- 진행 단계:
  - `5 / 6`
  - `Wallet Service 서명 요청` 활성
- 설명:
  - 사용자는 이 단계를 `실행 직전 보안 처리` 정도로 이해하면 충분하다.

### Step 6. 발행 실행 / 결과 기록
- 사용자 화면:
  - 발행 완료 배지
  - 발행 결과 요약
  - 이력 저장 상태
- 중앙 패널:
  - `발행이 실행되고 결과가 기록됩니다.`
  - 결과 요약과 이후 감사 추적 가능 여부를 보여준다.
- 진행 단계:
  - `6 / 6`
  - `발행 실행 / 결과 기록` 활성
- 설명:
  - 마지막 단계에서는 발행 성공 여부와 이력 저장을 함께 보여줘야 한다.

---

## FS-3. 소각 요청
- actor: `발행사 관리자`
- 화면 타입: `웹 관리자 콘솔`
- total steps: `6`

### Step 1. 소각 요청 생성
- 사용자 화면:
  - 소각 수량 입력
  - 소각 사유 입력
  - 요청 생성 버튼
- 중앙 패널:
  - `어떤 수량을 어떤 이유로 소각할지 정의합니다.`
  - 발행과 유사하지만 반대 방향 흐름임을 보여준다.
- 진행 단계:
  - `1 / 6`
  - `소각 요청 생성` 활성
- 설명:
  - 발행과 비슷한 UI를 유지하면 사용자가 쉽게 비교할 수 있다.

### Step 2. 대상 수량 확인
- 사용자 화면:
  - 소각 대상 수량
  - 현재 잔량
  - 영향 요약 카드
- 중앙 패널:
  - `소각 대상과 수량이 맞는지 확인합니다.`
  - 단순 검증 단계라는 느낌을 준다.
- 진행 단계:
  - `2 / 6`
  - `대상 수량 확인` 활성
- 설명:
  - 사용자가 실수 없이 요청을 확정하도록 만드는 확인 화면이다.

### Step 3. 1차 승인
- 사용자 화면:
  - Approval Service 상태 카드
  - 1차 승인자 카드
  - 2차 승인자 대기 상태
- 중앙 패널:
  - `소각 요청이 Approval Service의 첫 번째 승인 라인으로 전달되었습니다.`
  - 아직 서명 단계 전이라는 점을 명확히 보여준다.
- 진행 단계:
  - `3 / 6`
  - `1차 승인` 활성
- 설명:
  - 이 단계는 실행보다 `첫 번째 승인 완료 여부` 확인이 중요하다.

### Step 4. 2차 승인
- 사용자 화면:
  - 1차 승인 완료 배지
  - 2차 승인자 카드
  - approval group 완료 조건
- 중앙 패널:
  - `두 번째 승인까지 완료되어야 소각 서명 요청이 열린다`는 점을 보여준다.
  - 운영 통제 모델이 발행과 대칭이라는 느낌을 준다.
- 진행 단계:
  - `4 / 6`
  - `2차 승인` 활성
- 설명:
  - 이 단계에서 `2인 승인` 구조가 명확히 보여야 한다.

### Step 5. Wallet Service 서명 요청
- 사용자 화면:
  - approval complete 상태
  - 실행 준비 상태
  - 이벤트 ID
- 중앙 패널:
  - `소각 실행을 위한 보안 서명 단계가 진행됩니다.`
  - 기술 처리 전체보다 실행 전 상태를 강조한다.
- 진행 단계:
  - `5 / 6`
  - `Wallet Service 서명 요청` 활성
- 설명:
  - 사용자가 보는 핵심은 `이제 실제 소각 직전이다`라는 상태다.

### Step 6. 결과 기록
- 사용자 화면:
  - 소각 완료 상태
  - 결과 요약 카드
  - 이력 링크
- 중앙 패널:
  - `소각 결과가 기록되고 이력으로 남습니다.`
  - 발행 대비 어떤 값이 감소했는지 간단히 보여준다.
- 진행 단계:
  - `6 / 6`
  - `결과 기록` 활성
- 설명:
  - 발행/소각 비교 화면과 이어지면 운영자 이해도가 높다.

---

## FS-4. 준비금 유동성 관리 요청
- actor: `발행사 관리자`
- 화면 타입: `웹 관리자 콘솔`
- total steps: `6`

### Step 1. 요청 생성
- 사용자 화면:
  - 요청 유형 선택
  - 입출금 금액 입력
  - 노트 입력
- 중앙 패널:
  - `준비금 또는 유동성 관리 요청을 시작합니다.`
  - 아직 실행이 아니라 요청 생성 단계임을 보여준다.
- 진행 단계:
  - `1 / 6`
  - `요청 생성` 활성
- 설명:
  - 사용자는 이 단계에서 어떤 요청을 올리는지만 정의한다.

### Step 2. 사유 기록
- 사용자 화면:
  - 요청 사유 입력
  - 관련 메모
  - 첨부 증빙 요약
- 중앙 패널:
  - `왜 이 요청이 필요한지 기록합니다.`
  - 이후 audit 관점에서 이 값이 중요함을 설명한다.
- 진행 단계:
  - `2 / 6`
  - `사유 기록` 활성
- 설명:
  - 단순 입출금이 아니라 감사 가능한 요청이라는 점이 드러나야 한다.

### Step 3. 발행사 / 플랫폼 승인
- 사용자 화면:
  - 발행사 승인 상태
  - 플랫폼 승인 상태
  - 전체 승인 현황
- 중앙 패널:
  - `두 조직의 승인 상태를 병렬적으로 보여줍니다.`
  - 승인 순서가 고정되지 않았다는 점을 같이 설명한다.
- 진행 단계:
  - `3 / 6`
  - `발행사 / 플랫폼 승인` 활성
- 설명:
  - SDS 기준으로 승인 순서는 관계없다는 점을 단계 설명에 넣는다.

### Step 4. 원화계좌 입출금 처리
- 사용자 화면:
  - 출금 계좌 / 입금 계좌 정보
  - 처리 상태
  - 처리 결과 요약
- 중앙 패널:
  - `승인이 끝난 뒤 실제 원화계좌 처리로 이어집니다.`
  - 은행 처리 또는 외부 계좌 처리가 연결됨을 보여준다.
- 진행 단계:
  - `4 / 6`
  - `원화계좌 입출금 처리` 활성
- 설명:
  - 발행사의 출금 계좌는 사전 등록되어 있어야 한다는 조건을 붙인다.

### Step 5. audit log 기록
- 사용자 화면:
  - audit log 미리보기
  - request id
  - 상태 타임라인
- 중앙 패널:
  - `요청과 승인과 처리가 모두 기록됩니다.`
  - 이후 감사자가 무엇을 보게 되는지 연결해 준다.
- 진행 단계:
  - `5 / 6`
  - `audit log 기록` 활성
- 설명:
  - 이 단계는 `실행 결과`보다 `증빙 가능성`을 보여주는 역할이 강하다.

### Step 6. 이력 확인
- 사용자 화면:
  - 이력 리스트
  - 상세 진입 버튼
  - 최근 변경 상태
- 중앙 패널:
  - `운영 이력과 결과가 남아 있는 상태를 보여줍니다.`
  - 현재 상태를 한 번 더 정리한다.
- 진행 단계:
  - `6 / 6`
  - `이력 확인` 활성
- 설명:
  - 준비금 유동성 시나리오는 대시보드보다 이력 확인으로 마무리되는 편이 자연스럽다.

---

## FU-1. 지갑 생성 -> keygen
- actor: `개인 사용자`
- 화면 타입: `모바일 앱 / 슈퍼앱`
- total steps: `5`

### Step 1. 지갑 생성 요청
- 사용자 화면:
  - `새 지갑 만들기` CTA
  - 간단한 안내 문구
- 중앙 패널:
  - `지갑 생성을 시작합니다.`
  - 사용자는 보안 구조 전체보다 생성 시작 상태만 이해하면 된다.
- 진행 단계:
  - `1 / 5`
  - `지갑 생성 요청` 활성
- 설명:
  - 가장 중요한 CTA가 분명해야 한다.

### Step 2. 보안 키 생성 시작
- 사용자 화면:
  - 로딩 상태
  - `안전하게 지갑을 준비하는 중` 문구
- 중앙 패널:
  - `보안 키 생성이 시작되었습니다.`
  - 내부 프로토콜명은 숨기고 `분산 보안 처리` 정도로 표현한다.
- 진행 단계:
  - `2 / 5`
  - `보안 키 생성 시작` 활성
- 설명:
  - 사용자는 기술명을 몰라도 `안전하게 준비 중`이라는 감각을 가져가면 된다.

### Step 3. 생성 진행 상태 확인
- 사용자 화면:
  - 진행률
  - 완료 예상 상태
- 중앙 패널:
  - `지갑 생성이 진행 중입니다.`
  - 단계형 로딩 또는 상태 카드로 충분하다.
- 진행 단계:
  - `3 / 5`
  - `생성 진행 상태 확인` 활성
- 설명:
  - 불필요한 기술 용어 대신 상태 변화가 잘 보여야 한다.

### Step 4. 지갑 연결 완료
- 사용자 화면:
  - 지갑 준비 완료
  - 계정 연결 상태
- 중앙 패널:
  - `지갑이 계정에 연결되었습니다.`
  - 사용 가능한 상태로 바뀌었음을 보여준다.
- 진행 단계:
  - `4 / 5`
  - `지갑 연결 완료` 활성
- 설명:
  - 이 단계부터 사용자는 실제 지갑을 가진 것으로 인식한다.

### Step 5. 주소 매핑 완료
- 사용자 화면:
  - 주소
  - wallet id
  - 잔액 기본 카드
- 중앙 패널:
  - `지갑 정보가 사용자 화면에 표시됩니다.`
  - 최종 결과 화면으로 마무리한다.
- 진행 단계:
  - `5 / 5`
  - `주소 매핑 완료` 활성
- 설명:
  - 결과가 명확하게 보이는 종료 화면이 필요하다.

---

## FU-2. 거래 서명 요청
- actor: `개인 사용자`
- 화면 타입: `모바일 앱 / 슈퍼앱`
- total steps: `5`

### Step 1. 거래 요청 생성
- 사용자 화면:
  - 자산 선택
  - 금액 입력
  - `다음` 또는 `서명 요청` 버튼
- 중앙 패널:
  - `사용자가 어떤 거래를 보내려는지 정의합니다.`
  - 아직 실행 전이며 요청을 만드는 단계다.
- 진행 단계:
  - `1 / 5`
  - `거래 요청 생성` 활성
- 설명:
  - 서명 이전에는 사용자의 의도와 입력값이 먼저 확정된다.

### Step 2. 사용자 검증
- 사용자 화면:
  - 비밀번호 / 생체인증 / 인증 확인
  - 검증 진행 상태
- 중앙 패널:
  - `요청자가 실제 사용자 본인인지 확인합니다.`
  - 인증 성공 후 다음 단계로 넘어감을 보여준다.
- 진행 단계:
  - `2 / 5`
  - `사용자 검증` 활성
- 설명:
  - 이 단계는 기술 서명 이전의 사용자 확인 단계다.

### Step 3. signer quorum 구성
- 사용자 화면:
  - `서명 준비 중` 상태
  - 보안 처리 진행 배지
- 중앙 패널:
  - `서명에 필요한 보안 처리 구성이 준비됩니다.`
  - 내부 노드 수나 구조를 다 보여주기보다 `보안 처리 구성 완료` 정도로 설명한다.
- 진행 단계:
  - `3 / 5`
  - `signer quorum 구성` 활성
- 설명:
  - 일반 사용자는 어떤 노드가 참여하는지보다 `서명 준비 중` 상태만 이해하면 된다.

### Step 4. partial signature 생성
- 사용자 화면:
  - 진행 중 상태
  - 잠시만 기다려달라는 안내
- 중앙 패널:
  - `서명 조합 과정이 진행 중입니다.`
  - 보안 처리가 단계적으로 이뤄지는 느낌만 전달하면 충분하다.
- 진행 단계:
  - `4 / 5`
  - `partial signature 생성` 활성
- 설명:
  - 기술 용어는 문서상 남기되, 실제 UI 문구는 더 부드럽게 바꾸는 것이 좋다.

### Step 5. raw signature 반환
- 사용자 화면:
  - 서명 완료 상태
  - 거래 요청 결과 요약
- 중앙 패널:
  - `서명 결과가 준비되었습니다.`
  - 상위 플랫폼이 이후 제출을 이어받는다는 경계를 짧게 보여준다.
- 진행 단계:
  - `5 / 5`
  - `raw signature 반환` 활성
- 설명:
  - 이 화면은 기술적으로는 서명 반환 단계지만, 사용자에게는 `거래 승인 완료`에 가까운 경험으로 보이는 편이 자연스럽다.

## 구현 메모
- `PO-1`, `IS-1`, `CU-*`, `FS-*`, `FU-*`는 모두 상단 시나리오 selector에서 전환 가능해야 한다.
- 좌측 mock은 actor에 따라 자동으로 바뀌어야 한다.
- `개인 사용자` 시나리오는 항상 `앱 화면`, `플랫폼 운영자 / 발행사 관리자 / 수탁 운영자 / 법인 사용자` 시나리오는 항상 `웹 화면`으로 고정하는 편이 좋다.
- `수탁` 시나리오는 법인 사용자 요청 화면도 모바일보다 `웹 포털` 느낌으로 잡는 것이 자연스럽다.
- 발행 / 소각 / 수탁 출금에는 `Approval Service` 패널이 먼저 오고, 그 다음에 `Wallet Service 서명 요청` 패널이 와야 한다.
- `두 명이 승인했다`는 사실은 단순 텍스트보다 `1차 승인 완료`, `2차 승인 완료`, `approval group satisfied` 같은 상태 카드로 보여주는 편이 좋다.
- 중앙 패널은 처음부터 복잡한 diagram을 넣지 말고, 텍스트 + 단순 박스 + 상태 배지 조합으로 시작하는 편이 안정적이다.
- 우측 `진행 단계`는 이 문서의 step 순서를 그대로 반영하면 된다.
